import { CompleteFilterPipe } from './complete-filter.pipe';

describe('CompleteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompleteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
